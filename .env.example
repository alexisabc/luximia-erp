# =============================================================================
# LUXIMIA ERP - ENVIRONMENT VARIABLES (TEMPLATE)
# Copy this file to .env and fill in your local/production values.
# =============================================================================

# -----------------------------------------------------------------------------
# 1. CORE SYSTEM SETTINGS
# -----------------------------------------------------------------------------
# Set to 'True' for local development, 'False' for production.
DEVELOPMENT_MODE=True
# Generates detailed error pages. Keep True in dev, False in prod.
DEBUG=True
# Cryptographic signing key. KEEP SECRET. Use a long random string.
SECRET_KEY=change_me_to_a_secure_random_string_in_production
# Django settings module path
DJANGO_SETTINGS_MODULE=luximia_erp.settings
# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# 2. DATABASE CONFIGURATION (PostgreSQL)
# -----------------------------------------------------------------------------
# Standard connection info for Docker/Local
POSTGRES_DB=luximiadb
POSTGRES_USER=luximiauser
POSTGRES_PASSWORD=luximiapass
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Production Override (e.g. Render/Railway/Heroku)
# If DATABASE_URL is set, it overrides the above fields.
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# SSL Requirement for DB connection (usually True in cloud providers)
DATABASE_SSL_REQUIRE=False

# -----------------------------------------------------------------------------
# 3. BACKEND NETWORKING & SECURITY
# -----------------------------------------------------------------------------
# The explicit domain where the backend is hosted (e.g., api.luximia.mx)
ROOT_DOMAIN=
# Comma-separated list of valid host headers
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,backend

# -----------------------------------------------------------------------------
# 4. FRONTEND & CORS CONFIGURATION
# -----------------------------------------------------------------------------
# The domain of the frontend application (used for links, CORS, etc.)
FRONTEND_DOMAIN=localhost:3000
# Allowed Origins for CORS requests (comma separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# 5. FRONTEND CONNECTIVITY (Next.js)
# -----------------------------------------------------------------------------
# URL used by the BROWSER to access the API (Public URL)
NEXT_PUBLIC_API_URL=http://localhost:8000/
# URL used by the SERVER (Next.js SSR) to access the API (Internal Docker Network)
API_URL=http://backend:8000/

# -----------------------------------------------------------------------------
# 6. AUTHENTICATION & PASSKEYS (WebAuthn)
# -----------------------------------------------------------------------------
# JWT Token Lifetimes
ACCESS_TOKEN_LIFETIME_MINUTES=60
REFRESH_TOKEN_LIFETIME_DAYS=7

# WebAuthn / Passkeys Settings
# The Relying Party ID. In dev: localhost. In prod: domain.com (no port, no protocol)
RP_ID=localhost
# The Origin. Must match exactly what the browser sends (protocol + domain + port)
WEBAUTHN_ORIGIN=http://localhost:3000
RP_NAME="Luximia ERP"
# User Verification requirement (True/False)
PASSKEY_STRICT_UV=True

# -----------------------------------------------------------------------------
# 7. EMAIL SETTINGS
# -----------------------------------------------------------------------------
# Development (MailHog)
EMAIL_HOST=mailhog
EMAIL_PORT=1025
DEFAULT_FROM_EMAIL=desarrollo@luximia.local

# Production (SendGrid)
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# SENDGRID_API_KEY=your_sendgrid_api_key
# SENDGRID_FROM_EMAIL=notifications@yourdomain.com

# -----------------------------------------------------------------------------
# 8. STORAGE & ASSETS (Cloudflare R2 / AWS S3)
# -----------------------------------------------------------------------------
# Local static assets path (Docker internal)
ASSETS_PATH=/app/static/assets

# Cloud Storage Credentials
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET_NAME=
CLOUDFLARE_R2_ENDPOINT_URL=
CLOUDFLARE_R2_REGION=auto
CLOUDFLARE_R2_ADDRESSING_STYLE=virtual

# -----------------------------------------------------------------------------
# 9. EXTERNAL INTEGRATIONS & MANAGEMENT
# -----------------------------------------------------------------------------
# Banxico Token for Exchange Rates
BANXICO_TOKEN=
# OpenAI (Optional)
# OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# 10. LOCAL TOOLS & SUPERUSER
# -----------------------------------------------------------------------------
# PGAdmin Credentials (Local Dev Only)
PGADMIN_EMAIL=admin@luximia.com
PGADMIN_PASSWORD=admin

# Automatically created superuser variables
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_FIRST_NAME=Admin
DJANGO_SUPERUSER_LAST_NAME=User
