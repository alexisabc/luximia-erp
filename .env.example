# Archivo de Ejemplo de Variables de Entorno para Luximia ERP
# Copia este archivo, renombralo a .env y rellena los valores para tu entorno.

# -----------------------------------------
# Configuración de Django y Entorno
# -----------------------------------------
# Genera una clave segura para producción. Puedes usar la de Django por defecto para desarrollo.
SECRET_KEY=cambiame_por_una_clave_segura_y_larga

# Apunta al archivo de configuración unificado. No cambies esto.
DJANGO_SETTINGS_MODULE=luximia_erp.settings

# En producción, pon esto en 'False'. En desarrollo, 'True'.
DEBUG=True
DEVELOPMENT_MODE=True

# En producción, pon aquí tus dominios: "mi-app.onrender.com,www.mi-app.com"
ALLOWED_HOSTS=localhost,127.0.0.1

# En producción, pon la URL de tu frontend: "https://www.mi-app.com"
CORS_ALLOWED_ORIGINS=http://localhost:3000


# -----------------------------------------
# Base de Datos PostgreSQL
# -----------------------------------------
# URL completa para producción (ej. la que te da Render).
DATABASE_URL=postgres://user:password@hostname:5432/database_name

# Variables individuales para desarrollo local con Docker Compose.
POSTGRES_DB=luximiadb
POSTGRES_USER=luximiauser
POSTGRES_PASSWORD=luximiapass
POSTGRES_HOST=db
POSTGRES_PORT=5432


# -----------------------------------------
# URLs de Frontend y Backend
# -----------------------------------------
# Dominio del frontend para construir enlaces en correos. En producción, "app.luximia.mx".
FRONTEND_DOMAIN=localhost:3000

# URL que usa el NAVEGADOR para hablar con el backend.
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# URL que usa el SERVIDOR de Next.js para hablar con el backend (dentro de Docker).
API_URL=http://backend:10000/api


# -----------------------------------------
# Creación de Superusuario
# -----------------------------------------
# Email para el superusuario inicial que se creará con el comando automatizado.
DJANGO_SUPERUSER_EMAIL=admin@tu-empresa.com


# -----------------------------------------
# Lógica de Negocio y APIs Externas
# -----------------------------------------
BANXICO_TOKEN=pega_aqui_tu_token_de_banxico
# OPENAI_API_KEY=pega_aqui_tu_token_de_openai # Descomenta si usas OpenAI
ASSETS_PATH=/app/static/assets
LOG_LEVEL=INFO


# -----------------------------------------
# Email para Desarrollo (MailHog)
# -----------------------------------------
# En producción, utiliza los datos de Azure Communication Services.
EMAIL_HOST=mailhog
EMAIL_PORT=1025
DEFAULT_FROM_EMAIL=noreply@luximia.local

# Variables para Azure Communication Services
AZURE_COMMUNICATION_CONNECTION_STRING=tu_cadena_de_conexion
AZURE_COMMUNICATION_SENDER_ADDRESS=tu_direccion_verificada@tudominio.com